# RankUpAPI Production Environment Configuration
# Copy this to docker/.env for production deployment

# Database Configuration
MSSQL_SA_PASSWORD=RankUp@ProdPass2026
MSSQL_PORT=7864

# Gateway Port (External Access)
GATEWAY_PORT=5087

# Environment
ASPNETCORE_ENVIRONMENT=Docker

# All connection strings will use Docker internal network
# No need to change these for Docker deployment
# They automatically use: Server=sqlserver;Database=XXX;User Id=sa;Password=${MSSQL_SA_PASSWORD}
