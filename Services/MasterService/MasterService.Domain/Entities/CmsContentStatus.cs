namespace MasterService.Domain.Entities
{
    /// <summary>
    /// CMS content visibility status. Maps directly to schema - use in API requests/responses.
    /// </summary>
    public enum CmsContentStatus
    {
        Inactive = 0,
        Active = 1
    }
}
